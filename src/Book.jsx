import React, { useEffect, useState } from "react";

export default function Llibre(props) {
  let infoLlibre = {
    titol: props.titol,
    categoria: props.categoria,
    any: props.any,
    pagines: props.pagines,
    estat: props.estat,
  };
}
